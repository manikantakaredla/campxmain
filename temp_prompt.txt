<USER_REQUEST>
Role: You are a senior full-stack developer. You will receive a project (CampX). Your task is to implement the following changes strictly in the order of phases below. After completing each phase, you must stop and run the specified tests for that phase. Do not proceed to the next phase until the current phase is 100% functional and tested. The goal is a fully working system from frontend to backend with no broken flows.

Global Rule for ALL Changes: Before writing any code, first analyze the existing codebase to understand the current data models, API endpoints, and component structures. Then, implement changes. After each phase, fix any errors introduced.

PHASE 1: LOGIN & AUTHENTICATION (CRITICAL FIXES)
Objective: Fix the core login flow, replace email with Roll No/Emp ID, implement forgot password, and fix remember me.

Changes:

Login Form: Remove the email field. Replace it with rollNoOrEmpId (label: "Roll No / Employee ID").

Backend Login Logic: Modify the login API to check against rollNo (for students) or empId (for faculty/admin/management). Return specific error messages:

If user not found: { error: "User not found. Please register." }

If password wrong: { error: "Invalid credentials. Please check your password." }

Forgot Password Implementation:

Add "Forgot Password?" link.

Flow: Enter Roll No/Emp ID -> Send OTP to registered email/phone -> Verify OTP -> Set new password.

Create new API endpoints: /forgot-password, /verify-otp, /reset-password.

Remember Me: Fix the "Remember Me" checkbox functionality so it actually persists the session beyond browser close using secure HTTP-only cookies with a longer expiry (e.g., 7 days).

TESTING FOR PHASE 1:

Try login with wrong Roll No/Emp ID → See "User not found. Please register."

Try login with correct ID but wrong password → See "Invalid credentials..."

Try login with correct ID and password → Successful login.

Click "Remember Me", login, close browser, reopen → Should stay logged in.

Te
<truncated 8648 bytes>
an internship announcement (auto) → Also appears in calendar, no duplicate.

PHASE 9: FINAL SYSTEM INTEGRATIONS & FLAW CHECK
Objective: Ensure the entire flow works end-to-end and no flaws remain.

Changes & Checks:

Branch/Section Consistency: Ensure that branches/sections added in Settings (Phase 5) appear everywhere: User Management, Announcements target audience, Resource filters.

Role-Based Department Restriction:

HOD/Faculty can only upload announcements/resources for their own department.

Admin can upload for any department.

Management can upload for their managed branches.

Upload User (Bulk/Individual): Implement page where admin can add users individually OR via CSV bulk upload. CSV must support rollNo, name, email, branch, section, role.

Final Flaw Scan: Check for any broken links, missing API endpoints, console errors, or UI misalignments. Fix all.

TESTING FOR PHASE 9:

Add a new branch "AIDS" in Settings → It appears in User Management and Announcements.

Login as CSE faculty → Try to upload announcement for ECE → Should be blocked.

Upload users via CSV → All users created correctly.

Run through entire app: Login → Dashboard → Announcements → Resources → Profile → Logout. No errors.

FINAL NOTE FOR ANTIGRAVITY:
After completing Phase 9, run a full regression test covering:

All user roles (Student, Admin, Faculty, Management)

All CRUD operations

All search/filter/pagination

All notification flows

All calendar events

Provide a summary report at the end: "✅ 100% of requested changes implemented and tested. No missing flows."
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-06-09T19:17:16+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Gemini 3.5 Flash (Medium). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>